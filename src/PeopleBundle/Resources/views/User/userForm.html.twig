{% block content %}
    <h1>Create Player Form</h1>
    {% if (form is not null) %}
        {{form_start(form)}}
            {{ form_widget(form) }}
            <input type="submit" value="submit">
        {{form_end(form)}}
    {% endif %}
{% endblock %}